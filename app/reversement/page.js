export default function ReversementPage() {
  return (
    <div className="max-w-3xl mx-auto px-4 py-16 text-center">
      <h1 className="font-serif text-xl font-bold mb-2" style={{ color: 'var(--text)' }}>
        Reversement
      </h1>
      <p className="text-sm" style={{ color: 'var(--text3)' }}>
        Halaman ini sedang dalam proses migrasi dari reversement.html (pagination, search, reaction emoji).
        Menyusul di sesi berikutnya.
      </p>
    </div>
  );
}
